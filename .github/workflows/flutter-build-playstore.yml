name: Upload to Google Play Store

on:
  workflow_dispatch:
    inputs:
      TRACK:
        description: 'Track for Play Store release'
        required: true
        default: 'internal'
        type: choice
        options:
          - internal
          - alpha
          - beta
          - production
      STATUS:
        description: 'Release status for Play Store'
        required: true
        default: 'draft'
        type: choice
        options:
          - draft
          - completed
          - inProgress
          - halted

env:
  APK_NAME: TeacherApp
  ENVIRONMENT: production
  DEBUG_INFO_DIR: debug_info
  PACKAGE_NAME: com.padma.yoglog
  KEYSTORE_PATH: /tmp/keystore/key_store_file.jks

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java (JDK 17)
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.3'
          channel: 'stable'

      - name: Set up Ruby (Bundler + Fastlane)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: false

      - name: Install Bundler and Gems
        working-directory: android
        run: |
          gem install bundler -v 2.7.2
          bundle _2.7.2_ install

      - name: Verify environment
        run: |
          flutter --version
          flutter doctor -v
          cd android
          bundle _2.7.2_ exec fastlane --version

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Generate Env File
        run: |
          echo "DB_NAME=app.db" > .env.production
          echo 'APP_NAME="Personal Tuition Manager"' >> .env.production

      - name: Create Debug Info Directory
        run: mkdir -p "android/build/${DEBUG_INFO_DIR}"

      - name: Build App Bundle (.aab)
        working-directory: android
        run: |
          flutter build appbundle --release \
            --obfuscate \
            --split-debug-info="build/${DEBUG_INFO_DIR}" \
            --dart-define=ENV=${ENVIRONMENT} \
            --build-name="${GITHUB_RUN_NUMBER}" \
            --build-number=${GITHUB_RUN_NUMBER}

      - name: Prepare Keystore
        run: |
          mkdir -p "$(dirname "$KEYSTORE_PATH")"
          echo "${{ secrets.KEYSTORE_FILE }}" | base64 -d > "$KEYSTORE_PATH"
          ls -la "$(dirname "$KEYSTORE_PATH")"

      - name: Prepare Play Store JSON Key
        run: |
          if [ -z "${{ secrets.PLAY_STORE_JSON_KEY }}" ]; then
            echo "::error::PLAY_STORE_JSON_KEY secret is missing!"
            exit 1
          fi
          echo "${{ secrets.PLAY_STORE_JSON_KEY }}" | base64 -d > android/fastlane/play_store_json_key.json
          echo "PLAY_STORE_JSON_PATH=$(pwd)/android/fastlane/play_store_json_key.json" >> $GITHUB_ENV
          ls -la android/fastlane/play_store_json_key.json

      - name: Upload to Play Store (Fastlane)
        working-directory: android
        env:
          AAB_FILE_PATH: android/build/app/outputs/bundle/release/app-release.aab
          PLAY_STORE_JSON_PATH: ${{ env.PLAY_STORE_JSON_PATH }}
          FASTFILE_TRACK: ${{ github.event.inputs.TRACK }}
          FASTFILE_RELEASE_STATUS: ${{ github.event.inputs.STATUS }}
          PACKAGE_NAME: ${{ env.PACKAGE_NAME }}
        run: |
          echo "Uploading $AAB_FILE_PATH to Play Store track $FASTFILE_TRACK (status: $FASTFILE_RELEASE_STATUS)"
          bundle _2.7.2_ exec fastlane deploy
          rm -f fastlane/play_store_json_key.json
          echo "::notice::Cleaned up Play Store JSON key"

      - name: Cleanup Keystore
        if: always()
        run: rm -f "$KEYSTORE_PATH"

      - name: Final Status
        if: always()
        run: |
          if [[ "${{ job.status }}" == "success" ]]; then
            echo "::notice::✅ Build completed successfully and uploaded to Play Store!"
          else
            echo "::error::❌ Build or upload failed!"
          fi
